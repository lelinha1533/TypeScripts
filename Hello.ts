function greeter(person:string) {
    return "Hello, " + person;
  }
   
  let user = "Jane User";
   
const info = {
    name : "Nguyen Van Cuong",
    domain : "Freetuts.net"
};
  
console.log(info);

  
  //document.body.textContent = greeter(user);

  // compile:
  // tsc greeter.ts